import React, { Component } from 'react';

import { Container, Content, Text, View, Body } from 'native-base';

import styles from './styles';

export default class TabCalendar extends Component {
	// eslint-disable-line

	render() {
		// eslint-disable-line
		return (
			<Content padder>
				<Text>Ummmmmm is it worked?</Text>
			</Content>
		);
	}
}
